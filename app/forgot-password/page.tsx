'use client';
import {FormEvent,useState} from 'react';import {createClient} from '@/lib/supabase/client';
export default function Forgot(){const [email,setEmail]=useState('');const [msg,setMsg]=useState('');const [loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);const {error}=await createClient().auth.resetPasswordForEmail(email,{redirectTo:`${window.location.origin}/reset-password`});setMsg(error?error.message:'Se o e-mail existir, enviaremos as instruções de recuperação.');setLoading(false)}return <main className="auth-page"><form onSubmit={submit} className="auth-card"><div className="brand"><span>GP</span><div><strong>GestorPro</strong><small>2.0</small></div></div><h1>Recuperar senha</h1><p>Informe seu e-mail para receber o link.</p><label>E-mail<input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label>{msg&&<div className="error">{msg}</div>}<button disabled={loading}>{loading?'Enviando...':'Enviar link'}</button><a href="/login">Voltar ao login</a></form></main>}

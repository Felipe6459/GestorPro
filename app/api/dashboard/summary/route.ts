import { NextResponse } from 'next/server';
import { createClient } from '@/lib/supabase/server';
export async function GET(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)return NextResponse.json({error:'Não autenticado'},{status:401});const {data:m}=await s.from('organization_members').select('organization_id').eq('user_id',user.id).limit(1);const org=m?.[0]?.organization_id;if(!org)return NextResponse.json({clients:0,active:0,expiring:0,expired:0,revenue:0});const {data,error}=await s.from('clients').select('status,amount,due_date').eq('organization_id',org);if(error)return NextResponse.json({error:error.message},{status:500});const now=new Date();now.setHours(0,0,0,0);const limit=new Date(now);limit.setDate(limit.getDate()+7);const rows=data??[];const dates=rows.filter(x=>x.due_date).map(x=>new Date(`${x.due_date}T00:00:00`));return NextResponse.json({clients:rows.length,active:rows.filter(x=>x.status==='active').length,expired:rows.filter(x=>x.status==='expired'||(x.due_date&&new Date(`${x.due_date}T00:00:00`)<now)).length,expiring:dates.filter(d=>d>=now&&d<=limit).length,revenue:rows.reduce((n,x)=>n+Number(x.amount||0),0)});}

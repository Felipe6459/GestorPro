'use client';
import {FormEvent,useEffect,useState} from 'react';import {useRouter} from 'next/navigation';
import ClientManager from './ClientManager';
export default function Clients(){const router=useRouter();const [name,setName]=useState('');const [whatsapp,setWhatsapp]=useState('');const [plan,setPlan]=useState('');const [amount,setAmount]=useState('');const [due,setDue]=useState('');const [saving,setSaving]=useState(false);const [error,setError]=useState('');const [refresh,setRefresh]=useState(0);useEffect(()=>{},[refresh]);async function add(e:FormEvent){e.preventDefault();setSaving(true);setError('');const r=await fetch('/api/clients',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,whatsapp,plan,amount,due_date:due})});const data=await r.json();if(!r.ok)setError(data.error||'Não foi possível cadastrar.');else{setName('');setWhatsapp('');setPlan('');setAmount('');setDue('');setRefresh(x=>x+1);router.refresh()}setSaving(false)}return <main className="dashboard"><header><div className="brand"><span>GP</span><div><strong>GestorPro</strong><small>2.0</small></div></div><a href="/dashboard" style={{color:'#c4b5fd'}}>Dashboard</a></header><section className="hero"><p className="eyebrow">CLIENTES</p><h1>Clientes</h1><p>Cadastre e acompanhe suas assinaturas.</p></section><form onSubmit={add} className="auth-card" style={{margin:'0 5% 25px',maxWidth:'none'}}><div style={{display:'grid',gridTemplateColumns:'repeat(3,1fr)',gap:12}}><label>Nome<input required value={name} onChange={e=>setName(e.target.value)}/></label><label>WhatsApp<input value={whatsapp} onChange={e=>setWhatsapp(e.target.value)}/></label><label>Plano<input value={plan} onChange={e=>setPlan(e.target.value)} placeholder="Mensal"/></label><label>Valor<input type="number" step="0.01" min="0" value={amount} onChange={e=>setAmount(e.target.value)}/></label><label>Vencimento<input type="date" value={due} onChange={e=>setDue(e.target.value)}/></label><button disabled={saving} style={{alignSelf:'end',height:44}}>{saving?'Salvando...':'Adicionar cliente'}</button></div>{error&&<div className="error">{error}</div>}</form><section style={{margin:'0 5%'}}><ClientManager key={refresh}/></section></main>}

'use client';
import {FormEvent,useState} from 'react';
export default function ClientForm({onSaved}:{onSaved:()=>void}){const [name,setName]=useState('');const [whatsapp,setWhatsapp]=useState('');const [plan,setPlan]=useState('');const [amount,setAmount]=useState('');const [due,setDue]=useState('');const [error,setError]=useState('');const [saving,setSaving]=useState(false);async function submit(e:FormEvent){e.preventDefault();setSaving(true);setError('');const r=await fetch('/api/clients',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,whatsapp,plan,amount,due_date:due})});const data=await r.json();if(!r.ok)setError(data.error||'Não foi possível cadastrar.');else{name&&setName('');setWhatsapp('');setPlan('');setAmount('');setDue('');onSaved()}setSaving(false)}return <form onSubmit={submit} className="auth-card" style={{maxWidth:'none'}}><h2>Novo cliente</h2><div style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(180px,1fr))',gap:12}}><label>Nome<input required value={name} onChange={e=>setName(e.target.value)} placeholder="Nome do cliente"/></label><label>WhatsApp<input value={whatsapp} onChange={e=>setWhatsapp(e.target.value)} placeholder="(00) 00000-0000"/></label><label>Plano<input value={plan} onChange={e=>setPlan(e.target.value)} placeholder="Mensal"/></label><label>Valor<input type="number" min="0" step="0.01" value={amount} onChange={e=>setAmount(e.target.value)} placeholder="0,00"/></label><label>Vencimento<input type="date" value={due} onChange={e=>setDue(e.target.value)}/></label></div>{error&&<div className="error">{error}</div>}<button disabled={saving}>{saving?'Cadastrando...':'Cadastrar cliente'}</button></form>}
